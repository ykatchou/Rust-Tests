use std::collections::BTreeMap;
mod graph_lib;
use graph_lib::*;

fn main() {
    println!("Hello, world!");
    
    //nodes.insert("paie1".to_string(), Node::new(NodeType::PayRule, "rule1", "fam1", "subfam1", 0));
    //nodes.insert("paie2".to_string(), Node::new(NodeType::PayRule, "rule2", "fam1", "subfam1", 0));

    //let n1 = nodes.get("paie1");
    //let n2 = nodes.get("paie2");
/*
    match n1{
        None => (),
        Some(sourcenode) => 
            match n2{
                Some(targetnode) => vertices.push(Vertex::new(sourcenode, targetnode)),
                None => ()
            }
        }
*/
    

}
